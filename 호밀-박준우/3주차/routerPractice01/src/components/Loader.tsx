const Loader = () => {
    return (
        <div className="flex justify-center items-center h-96">
            <div className="text-xl font-semibold animate-pulse">로딩 중...</div>
        </div>
    );
};

export default Loader;
